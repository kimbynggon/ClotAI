meta {
  name: 프로필 조회
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/profile
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
